using System;

// ReSharper disable once CheckNamespace
namespace Surveillance
{
    public static class MathExtensions
    {
        public static int Floor(this double d) => (int)Math.Floor(d);
    }
}